# SPDX-License-Identifier: MIT
"""Strategy plugins and the framework they build on.

This package re-exports nothing: import each name from the module that
defines it (``catalogue``, ``factory``, ``strategy_base``, ``shared_entry``,
``shared_exit``, a plugin's own ``strategy`` / ``screener``, or a foundation
module such as ``reasons``).
"""
